# Voorjaar 2025 Banner Generator

## Description

This is a banner generator for ING

## Installation

```
npm install
```

## Usage

```
npm start
```

Then edit `banners.json` to add all variants or edit the sizes.

## TODO

- [x] add all variants
- [x] generate a fallback image
- [x] add compression to generated images to reduce size
- [x] [add positioning to the generated images](https://sharp.pixelplumbing.com/api-resize/)
- [x] Fix the button
- [x] generate preview page automatically
- [x] only add files that are needed for the banner
- [x] heading font change
- [x] Calculate ID size and margin based on ID size
- [x] set motion to correct curve
- [x] compress banners to .zip files automatically
